import { reactEvents, reactAttributes } from '.'

describe('reactEvents', () => {
  it('is truthy', () => {
    expect(reactEvents).toBeTruthy()
  })
})

describe('reactAttributes', () => {
  it('is truthy', () => {
    expect(reactAttributes).toBeTruthy()
  })
})
